{
  "policies": [
    {
      "id": "rename-upsert-target-absent",
      "name": "Rename attribute with upsert when target does not exist (rename)",
      "log": {
        "match": [{ "log_field": "severity_text", "exact": "INFO" }],
        "keep": "all",
        "transform": {
          "rename": [
            { "from_log_attribute": "temp_id", "to": "user_id", "upsert": true }
          ]
        }
      }
    }
  ]
}
